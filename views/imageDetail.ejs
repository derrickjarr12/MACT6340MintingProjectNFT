<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= image.title %> | Gallery</title>
  <link rel="icon" type="image/x-icon" href="/images/SUN.ico"/>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous"/>
  <link rel="stylesheet" href="/css/style.css"/>
</head>
<body>
  <%- include('partials/header'); %>
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-lg-8 text-center">
        <h1 class="mb-4 display-4"><%= image.title %></h1>
        <img src="<%= image.src %>" alt="<%= image.title %>" class="img-fluid rounded shadow mb-4" style="max-height:70vh;">
        <p class="lead mb-2"><%= image.desc %></p>
        <small class="text-muted d-block mb-4"><%= image.nft %></small>
        <a href="/gallery" class="btn btn-outline-primary"><i class="fas fa-arrow-left me-2"></i>Back to Gallery</a>
      </div>
    </div>
  </div>
  <%- include('partials/footer'); %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
</html>
