<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <link rel="icon" type="image/x-icon" href="/images/SUN.ico"/>
    <title>Distortion - Audio Reactive Art</title>
    <meta name="author" content="Derrick Jarrell"/>
    <meta name="description" content="Interactive audio-reactive particle distortion demo"/>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous"/>
    <link rel="stylesheet" href="/css/style.css"/>
    <style>
        body {
            margin: 0;
            padding: 0;
            background: #000;
            overflow-x: hidden;
        }
        
        #demo-container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 600px;
            padding: 20px;
            margin-bottom: 50px;
        }
        
        canvas {
            border: 2px solid #333;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5), 
                        0 5px 15px rgba(0, 0, 0, 0.3);
            display: block;
            max-width: 100%;
            height: auto;
        }
        
        .controls {
            position: absolute;
            top: 120px;
            left: 20px;
            color: white;
            z-index: 10;
            background: rgba(0,0,0,0.8);
            padding: 10px;
            border-radius: 8px;
            display: flex;
            flex-direction: column;
            gap: 8px;
        }
        
        #demo-container {
            position: relative;
        }
        
        .audio-status {
            color: #0f0;
            font-size: 0.85rem;
            text-align: center;
            padding: 4px 0;
        }
        
        .controls button {
            background: #333;
            color: white;
            border: none;
            padding: 8px 12px;
            cursor: pointer;
            border-radius: 5px;
            font-size: 0.9rem;
            min-width: 80px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
        }
        
        .controls button:hover {
            background: #555;
        }
        
        .controls button:disabled {
            background: #222;
            cursor: not-allowed;
            opacity: 0.5;
        }
        
        footer {
            background: #111;
            border-top: 1px solid #333;
        }
        
        footer .text-muted {
            color: #999 !important;
        }
        
        footer .nav-link:hover {
            color: #fff !important;
        }
        
        .audio-status {
            color: #0f0;
            font-size: 0.9rem;
            margin-top: 10px;
            text-align: center;
        }
    </style>
</head>
<body>
    <%- include('partials/header'); %>
    
    <div class="controls">
        <button id="playBtn" onclick="toggleAudio()">
            <i class="fas fa-play"></i> Play
        </button>
        <button id="pauseBtn" onclick="pauseAudio()" style="display:none;">
            <i class="fas fa-pause"></i> Pause
        </button>
        <button onclick="resetSketch()">
            <i class="fas fa-sync"></i> Reset
        </button>
        <div class="audio-status" id="status">Ready</div>
    </div>
    
    <div id="demo-container">
        <div id="sketch-container"></div>
    </div>
    
    <%- include('partials/footer'); %>
    
    <script src="https://cdnjs.cloudflare.com/ajax/libs/p5.js/1.7.0/p5.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/p5.js/1.7.0/addons/p5.sound.min.js"></script>
    <script src="/js/distortion.js"></script>
</body>
</html>
